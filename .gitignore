# Runtime data (written by /update, not source)
live_data.csv
*.tmp

# Python
__pycache__/
*.py[oc]
.venv/

# ForexLens の CSV は「コミット済みフォールバック」として追跡する
# (Render 無料プランは再起動でディスクが消えるため。predictions_latest.csv と同じ方式)
